# Provider Configuration
# Choose how to authenticate with Claude:
#   - api-key: Uses ANTHROPIC_API_KEY (traditional API access)
#   - subscription: Uses Claude Code CLI auth (claude login) for Pro/Max subscriptions
PROVIDER_MODE=api-key

# Claude API Configuration (required for api-key mode, optional for subscription mode)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# For subscription mode, authenticate using the Claude Code CLI:
#   1. Install the Claude Code CLI: npm install -g @anthropic-ai/claude-code
#      (Note: This is different from 'ai-sdk-provider-claude-code' which is used internally by Oracle)
#   2. Authenticate: claude login (select "Log in with your subscription account")
#   3. Run Oracle with subscription mode: oracle research -p subscription "your query"

# OpenAI API for TTS/STT (optional - uses OpenAI's Whisper and TTS)
OPENAI_API_KEY=your_openai_api_key_here

# Research Configuration
MIN_SOURCES_PER_TOPIC=10
MAX_SEARCH_DEPTH=3
ENABLE_VOICE=true

# Output Configuration
OUTPUT_DIR=./output
OUTPUT_FORMAT=markdown
